import { DynamicColorCardDirective } from './dynamic-color-card.directive';

describe('DynamicColorCardDirective', () => {
  it('should create an instance', () => {
    // const directive = new DynamicColorCardDirective();
    // expect(directive).toBeTruthy();
  });
});
